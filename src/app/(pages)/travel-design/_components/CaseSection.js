'use client';

import React from 'react';
import clsx from 'clsx';
import CustomPortableText from '@/components/CustomPortableText';
import Button from '@/components/Button';

export default function CaseSection({ data }) {
	const d = {
		introTitle: '',
	};
	const { introTitle, introHeading, introCta } = d || {};

	return <section className="p-desing__case"></section>;
}
